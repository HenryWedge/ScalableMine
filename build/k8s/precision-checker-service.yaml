apiVersion: v1
kind: Service
metadata:
  name: precision-checker
  labels:
    app: precision-checker
spec:
  type: ClusterIP
  selector:
    app: precision-checker
  ports:
    - name: coordination
      protocol: TCP
      port: 80
      targetPort: 8080